How did array methods like push() and filter() help you manipulate data in this activity?
push() helped me easily add new items to the list without rewriting the array.
filter() helped me search for items by only the ones that match what I type.

What challenges did you face when trying to filter and search for items in the array?
It was hard to understand how filter() works and how to write the condition inside it.
I also had issues with case sensitivity, so I had to use toLowerCase() to make the search work properly/

How would you modify this code to make it more efficient or user-friendly?
To make it more user-friendly I would add CSS and div to center html elements.